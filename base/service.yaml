apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: ClusterIP        # ClusterIP is enough, ingress handles external traffic
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80