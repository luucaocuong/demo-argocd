resources:
  - ../../base

nameSuffix: -dev

patches:
  - target:
      kind: Deployment
      name: nginx
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1

images:
  - name: docker.io/library/nginx
    newTag: "1.25"